---
import PublicLayout from "../layouts/PublicLayout.astro";
import ScrollReveal from "../components/ScrollReveal.astro";
import Countdown from "../components/Countdown.astro";
import ChatBot from "../components/ChatBot.astro";

---

<PublicLayout title="KKR RPPI | Kebangunan Rohani dalam Yesus Kristus" description="KKR RPPI mempersatukan pelajar, alumni, dan masyarakat dalam kebangunan rohani Yesus Kristus.">
  <div class="space-y-16 sm:space-y-24 pb-24">
    <!-- Hero -->
    <section id="home" class="relative min-h-[90vh] flex items-center justify-center overflow-hidden">
      <div class="hero-bg-container">
        <img src="/images/hero-bg.webp" alt="Hero Background" width="1920" height="1080" fetchpriority="high" class="h-full w-full object-cover" />
        <div class="absolute inset-0 bg-gradient-to-br from-kkr-dark/85 via-kkr-purple/40 to-kkr-maroon/20"></div>
      </div>

      <div class="relative mx-auto max-w-7xl px-4 sm:px-6 w-full">
        <div class="grid gap-12 lg:grid-cols-1 lg:items-center justify-items-center">
          <div class="space-y-8 text-white relative z-10 animate-fade-in text-center">
            <p id="hero-subtitle-label" class="section-text text-sm uppercase tracking-[0.3em]">Ibadah Kebaktian Kebangunan Rohani</p>
            <h1 id="hero-heading" class="hero-text-shadow text-5xl sm:text-6xl md:text-7xl xl:text-[6rem] font-black leading-[0.9] tracking-[-0.04em]">
              <span id="hero-line1" class="text-white">TRUST</span><br />
              <span id="hero-line2" class="text-kkr-gold">IN</span><br />
              <span id="hero-line3" class="text-kkr-light">GOD</span>
            </h1>
            <div id="hero-titles" class="space-y-1 max-w-xl mx-auto hidden">
              <p id="hero-title1" class="text-lg text-white/80"></p>
              <p id="hero-title2" class="text-xl text-kkr-red font-semibold"></p>
            </div>
            <div class="max-w-xl space-y-4 mx-auto">
              <p id="hero-verse" class="hero-copy text-lg sm:text-xl leading-9 max-w-3xl">Percayalah kepada TUHAN dengan segenap hatimu dan janganlah bersandar kepada pengertianmu sendiri.</p>
              <p id="hero-verse-ref" class="text-sm uppercase tracking-[0.24em] text-kkr-gold">Amsal 3:5</p>
            </div>
            <div id="hero-event-info" class="flex flex-wrap gap-x-6 gap-y-2 text-sm text-white/80 justify-center"></div>
            <div class="flex flex-wrap gap-4 justify-center">
              <a href="/registration" class="inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-kkr-maroon to-kkr-red px-7 py-4 text-sm font-semibold text-white transition hover:-translate-y-0.5">
                <span id="btn-hero-primary">Daftar Sekarang</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
              </a>
              <a href="#about" class="inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-7 py-4 text-sm font-semibold text-white transition hover:bg-white/20 hover:text-white">
                <span id="btn-hero-secondary">Info Acara</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Tentang Acara -->
    <ScrollReveal>
      <section id="about" class="scroll-mt-20 mx-auto max-w-7xl px-6">
        <div class="grid gap-10 lg:grid-cols-[1fr_0.8fr]">
          <div class="glass-card space-y-6 rounded-[2.5rem] p-8 sm:p-10">
            <div class="flex items-center gap-3">
              <span class="inline-block w-8 h-px bg-kkr-red"></span>
              <span id="about-section-label" class="text-sm font-semibold uppercase tracking-[0.25em] text-kkr-red">Tentang Acara</span>
            </div>
            <h2 id="about-heading" class="text-3xl sm:text-4xl font-semibold text-kkr-dark leading-tight">KKR dengan Sukacita & Syukur</h2>
            <div class="space-y-6 max-w-prose">
              <p id="about-desc1" class="text-kkr-dark/90 leading-8">KKR RPPI 2025/2026 adalah kebaktian kebangunan rohani yang mempersatukan pelajar, alumni, dan masyarakat dari berbagai sekolah di Kediri. Sebuah momen untuk merenungkan kasih Tuhan dan mengalami pembaharuan iman.</p>
              <p id="about-desc2" class="text-kkr-dark/90 leading-8">Bersama pembicara tamu dari Surabaya, kita akan merenungkan makna kebangunan rohani yang sesungguhnya — bahwa pertobatan dan pembaharuan membawa kita lebih dekat kepada Tuhan, sesuai dengan firman dalam 2 Tawarikh 7:14.</p>
            </div>
            <div id="about-cards" class="grid gap-4 sm:grid-cols-2 mt-6 hidden">
              <div class="rounded-xl bg-kkr-light/50 p-5 border border-kkr-light/60">
                <h4 id="about-card1-title" class="font-semibold text-kkr-dark"></h4>
                <p id="about-card1-desc" class="text-sm text-kkr-dark/70 mt-1 leading-relaxed"></p>
              </div>
              <div class="rounded-xl bg-kkr-light/50 p-5 border border-kkr-light/60">
                <h4 id="about-card2-title" class="font-semibold text-kkr-dark"></h4>
                <p id="about-card2-desc" class="text-sm text-kkr-dark/70 mt-1 leading-relaxed"></p>
              </div>
            </div>
            <blockquote id="about-blockquote" class="border-l-4 border-kkr-red pl-6 py-3 italic text-kkr-dark/80">
              "Jika umat-Ku yang disebut dengan nama-Ku merendahkan diri, berdoa dan mencari wajah-Ku, maka Aku akan mendengar dari sorga."
              <br/><span id="about-blockquote-ref" class="text-sm not-italic text-kkr-dark/60 font-medium">— 2 Tawarikh 7:14</span>
            </blockquote>

          </div>

          <div class="space-y-4">
            <div class="rounded-[2.5rem] border border-kkr-light/30 bg-white p-8 shadow-2xl shadow-black/30">
              <div class="space-y-8">
                <div class="flex items-center justify-between gap-4">
                  <span id="event-card-badge" class="inline-flex items-center gap-2 rounded-full bg-kkr-gold/15 px-4 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-kkr-red">Terbuka untuk umum</span>
                  <div class="flex items-center gap-3">
                    <img src="/logo-small.webp" alt="KKR RPPI" class="h-20 w-20 rounded-3xl border border-kkr-gold/40 bg-white/90 p-2 object-contain shadow-lg shadow-kkr-gold/20" />
                    <span id="event-card-brand" class="text-sm font-semibold text-kkr-dark">KKR RPPI</span>
                  </div>
                </div>
                <div class="space-y-3">
                  <p id="event-card-label-pada" class="text-xs uppercase tracking-[0.24em] text-kkr-maroon/60">Pada</p>
                  <h2 id="event-card-date" class="text-4xl font-semibold text-kkr-dark">Memuat...</h2>
                  <p id="event-card-venue" class="text-sm text-kkr-maroon/70">Memuat...</p>
                </div>
                <div class="grid gap-3">
                  <div class="rounded-[2rem] bg-kkr-dark/90 p-5 border border-white/10 h-24 flex flex-col justify-center">
                    <p id="event-card-label-pukul" class="text-xs uppercase tracking-[0.22em] text-kkr-gold/90">Pukul</p>
                    <p id="event-card-time" class="mt-2 text-2xl font-semibold text-white">Memuat...</p>
                  </div>
                  <div class="rounded-[2rem] bg-kkr-purple/90 p-5 border border-white/10 h-24 flex flex-col justify-center">
                    <p id="event-card-label-tempat" class="text-xs uppercase tracking-[0.22em] text-kkr-light">Tempat</p>
                    <p id="event-card-place" class="mt-2 text-2xl font-semibold text-white">Memuat...</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </ScrollReveal>

    <!-- Pembicara (dinamis dari DB) -->
    <ScrollReveal>
      <section id="speakers" class="scroll-mt-20 mx-auto max-w-7xl px-6">
        <div class="text-center max-w-2xl mx-auto space-y-4 mb-12">
          <div class="flex items-center justify-center gap-3">
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
            <span id="section-label-speakers" class="text-sm font-semibold uppercase tracking-[0.25em] text-white">Pembicara</span>
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
          </div>
          <h2 id="section-heading-speakers" class="text-3xl sm:text-4xl font-semibold text-white">Menghadirkan...</h2>
          <div class="w-12 h-0.5 bg-gradient-to-r from-kkr-red to-kkr-gold mx-auto rounded-full"></div>
        </div>

        <span id="section-label-story" class="hidden">Story Pembicara</span>

        <div id="speaker-grid" class="w-full max-w-5xl mx-auto">
          <div class="col-span-full text-center py-12 text-white/50" id="speaker-skeleton">
            <div class="inline-block w-8 h-8 border-2 border-kkr-maroon border-t-transparent rounded-full animate-spin"></div>
            <p class="mt-3 text-sm">Memuat pembicara...</p>
          </div>
        </div>
      </section>
    </ScrollReveal>

    <!-- Pelayan Altar -->
    <ScrollReveal>
      <section id="altar" class="scroll-mt-20 mx-auto max-w-7xl px-6">
        <div class="text-center max-w-2xl mx-auto space-y-4 mb-12">
          <div class="flex items-center justify-center gap-3">
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
            <span id="section-label-altar" class="text-sm font-semibold uppercase tracking-[0.25em] text-white">Pelayan Altar</span>
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
          </div>
          <h2 id="section-heading-altar" class="text-3xl sm:text-4xl font-semibold text-white">Pelayan Altar KKR RPPI</h2>
          <p id="section-desc-altar" class="section-text-muted max-w-xl mx-auto">Para pelayan altar yang melayani dalam kebangunan rohani</p>
          <div class="w-12 h-0.5 bg-gradient-to-r from-kkr-red to-kkr-gold mx-auto rounded-full"></div>
        </div>

        <div id="altar-grid" class="w-full">
          <div class="text-center py-12 text-white/50" id="altar-skeleton">
            <div class="inline-block w-8 h-8 border-2 border-kkr-maroon border-t-transparent rounded-full animate-spin"></div>
            <p class="mt-3 text-sm">Memuat pelayan altar...</p>
          </div>
        </div>
      </section>
    </ScrollReveal>

    <!-- Sponsor Section -->
    <ScrollReveal>
      <section id="sponsors" class="scroll-mt-20 mx-auto max-w-7xl px-6">
        <div class="text-center max-w-2xl mx-auto space-y-4 mb-12">
          <div class="flex items-center justify-center gap-3">
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
            <span id="section-label-sponsors" class="text-sm font-semibold uppercase tracking-[0.25em] text-white">Sponsor</span>
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
          </div>
          <h2 id="section-heading-sponsors" class="text-3xl sm:text-4xl font-semibold text-white">Sponsor By</h2>
          <p id="section-desc-sponsors" class="section-text-muted max-w-xl mx-auto">seluruh pihak yang telah dengan penuh kasih mendukung terselenggaranya KKR RPPI 2026</p>
          <div class="w-12 h-0.5 bg-gradient-to-r from-kkr-red to-kkr-gold mx-auto rounded-full"></div>
        </div>

        <!-- Sponsor Marquee -->
        <div id="sponsor-grid" class="overflow-hidden" style="mask-image: linear-gradient(to right, transparent 0, black 40px, black calc(100% - 40px), transparent 100%);">
          <div class="text-center py-12 text-white/50" id="sponsor-skeleton">
            <div class="inline-block w-8 h-8 border-2 border-kkr-maroon border-t-transparent rounded-full animate-spin"></div>
            <p class="mt-3 text-sm">Memuat sponsor...</p>
          </div>
          <div class="sponsor-track flex gap-6 hidden" style="width: max-content;">
            <div class="flex gap-6 items-stretch" data-copy="1"></div>
            <div class="flex gap-6 items-stretch" data-copy="2" aria-hidden="true"></div>
          </div>
        </div>

        <div class="text-center mt-10">
          <a href="#contact" class="inline-flex items-center gap-2 rounded-full bg-kkr-red px-8 py-4 text-sm font-semibold text-white transition hover:bg-kkr-maroon">
            <span id="btn-sponsor-contact">Tertarik jadi Sponsor? Hubungi Kami</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
          </a>
        </div>
      </section>
    </ScrollReveal>

    <!-- Countdown -->
    <Countdown />

    <!-- Lokasi -->
    <ScrollReveal>
      <section id="location" class="scroll-mt-20 mx-auto max-w-7xl px-6">
        <div class="text-center max-w-2xl mx-auto space-y-4 mb-12">
          <div class="flex items-center justify-center gap-3">
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
            <span id="section-label-location" class="text-sm font-semibold uppercase tracking-[0.25em] text-white">Lokasi & Waktu</span>
            <span class="inline-block w-8 h-px bg-kkr-red"></span>
          </div>
          <div class="w-12 h-0.5 bg-gradient-to-r from-kkr-red to-kkr-gold mx-auto rounded-full"></div>
        </div>

        <div class="grid gap-5 lg:grid-cols-2 items-stretch">
          <div class="rounded-2xl bg-kkr-dark/70 backdrop-blur-md p-4 sm:p-5 border border-white/10">
            <div class="flex items-center gap-2 mb-3">
              <span class="w-7 h-7 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0"><svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg></span>
              <span class="text-[10px] font-semibold uppercase tracking-[0.2em] text-white">Lokasi & Waktu</span>
            </div>
            <h2 id="loc-venue" class="text-lg sm:text-xl font-semibold text-white">Memuat...</h2>
            <div class="mt-3 grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
              <div class="flex items-start gap-2">
                <span class="w-6 h-6 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0 mt-0.5"><svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg></span>
                <div>
                  <p id="loc-label-alamat" class="text-[10px] font-semibold uppercase tracking-wider text-white/60">Alamat</p>
                  <p id="loc-address" class="text-xs text-white/90">Memuat...</p>
                </div>
              </div>
              <div class="flex items-start gap-2">
                <span class="w-6 h-6 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0 mt-0.5"><svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg></span>
                <div>
                  <p id="loc-label-tanggal" class="text-[10px] font-semibold uppercase tracking-wider text-white/60">Tanggal</p>
                  <p id="loc-date" class="text-xs text-white/90">Memuat...</p>
                </div>
              </div>
              <div class="flex items-start gap-2">
                <span class="w-6 h-6 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0 mt-0.5"><svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></span>
                <div>
                  <p id="loc-label-waktu" class="text-[10px] font-semibold uppercase tracking-wider text-white/60">Waktu</p>
                  <p id="loc-time" class="text-xs text-white/90">Memuat...</p>
                </div>
              </div>
              <div class="flex items-start gap-2">
                <span class="w-6 h-6 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0 mt-0.5"><svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v20M2 12h20"/></svg></span>
                <div>
                  <p id="loc-label-batas-daftar" class="text-[10px] font-semibold uppercase tracking-wider text-white/60">Batas Daftar</p>
                  <p id="reg-deadline" class="text-xs text-white/90">Memuat...</p>
                </div>
              </div>
            </div>
            <div class="mt-3 flex items-start gap-2 border-t border-white/10 pt-3">
              <span class="w-6 h-6 rounded-lg bg-white/10 flex items-center justify-center text-white shrink-0"><svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg></span>
              <div>
                <p id="loc-label-kontak" class="text-[10px] font-semibold uppercase tracking-wider text-white/60">Kontak</p>
                <p id="loc-phone" class="text-xs text-white/90">Memuat...</p>
                <a id="loc-wa" href="#" target="_blank" class="text-xs text-white/80 hover:text-white font-medium">Memuat...</a>
              </div>
            </div>
            <div class="mt-3">
              <a id="loc-maps-btn" href="#" target="_blank" class="inline-flex items-center gap-1.5 rounded-lg border border-white/20 bg-white/10 px-3 py-1.5 text-xs font-semibold text-white transition hover:bg-white/20">
                <span id="loc-btn-maps-text">Buka di Google Maps</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
              </a>
            </div>
          </div>

          <div class="overflow-hidden rounded-2xl glass-card h-full min-h-[280px]">
            <div class="relative h-full w-full">
              <iframe id="loc-maps-iframe" class="absolute inset-0 h-full w-full" src="" loading="lazy" referrerpolicy="strict-origin-when-cross-origin" style="filter:grayscale(0.15) contrast(1.05)"></iframe>
              <div class="pointer-events-none absolute inset-0 rounded-2xl ring-1 ring-inset ring-black/5"></div>
            </div>
          </div>
        </div>
      </section>
    </ScrollReveal>

    <!-- CTA -->
    <ScrollReveal>
      <section id="contact" class="mx-auto max-w-7xl px-6">
        <div class="rounded-[2.5rem] bg-gradient-to-br from-kkr-dark via-kkr-purple to-kkr-maroon p-8 sm:p-14 text-white shadow-2xl shadow-kkr-dark/30 relative overflow-hidden">
          <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,_rgba(251,207,232,.08),_transparent_50%)]"></div>
          <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom_right,_rgba(219,234,254,.05),_transparent_50%)]"></div>
          <div class="relative grid gap-8 lg:grid-cols-[1.3fr_0.9fr] lg:items-center">
            <div>
              <div class="flex items-center gap-3">
                <span class="inline-block w-8 h-px bg-kkr-red"></span>
                <span id="section-label-cta" class="text-sm font-semibold uppercase tracking-[0.3em] text-white/80">Siap Bergabung?</span>
              </div>
              <h2 id="cta-title" class="mt-4 text-3xl sm:text-4xl font-semibold">Daftar sekarang, dan ikuti KKR dalam kasih Kristus.</h2>
              <p id="cta-desc" class="mt-4 max-w-xl text-white/70 leading-relaxed">Isi formulir pendaftaran, pilih tiket, dan amankan tempatmu dalam KKR RPPI yang penuh sukacita dan berkat.</p>
            </div>
            <div class="space-y-4">
              <div class="rounded-xl border border-white/10 bg-white/5 p-5 backdrop-blur-sm">
                <p id="cta-contact-label" class="text-xs uppercase tracking-[0.2em] text-kkr-light font-semibold">Kontak</p>
                <p class="mt-2 text-lg font-semibold" id="cta-email">kkrrppi@gmail.com</p>
                <p class="text-white/70 text-sm" id="cta-phone">Memuat...</p>
              </div>
            </div>
          </div>
        </div>
      </section>
    </ScrollReveal>
  </div>

  <!-- Floating Components -->
  <ChatBot />

  <script>
    function escapeHtml(str) {
      if (!str) return "";
      return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
    }

    async function setEmbedMap(venue, mapsLink) {
      try {
        var params = "venue=" + encodeURIComponent(venue || "Lokasi Acara");
        if (mapsLink) params += "&mapsLink=" + encodeURIComponent(mapsLink);
        var res = await fetch("/api/geocode?" + params);
        var data = await res.json();
        return { url: data?.url || "" };
      } catch(e) {
        return { url: "" };
      }
    }

    function sanitizeUrl(url) {
      if (!url) return "";
      try {
        const parsed = new URL(url, window.location.origin);
        if (parsed.protocol !== "http:" && parsed.protocol !== "https:") return "";
        return parsed.href;
      } catch { return ""; }
    }

    function safe(id) { return document.getElementById(id); }

    async function loadSettings() {
      try {
        const s = await (window.__SETTINGS_CACHE || fetch("/api/settings").then(r => r.json()).then(d => d.settings || {}));
        const venue = s.locVenue || "Kediri";
        const date = s.locDate || "Sabtu, 10 Januari 2026";
        const time = s.locTime || "08:00 - 12:30 WIB";
        const regDeadline = s.regDeadline || "29 Desember 2025";
        const mapsLink = s.locMapsLink || "https://maps.app.goo.gl/RCzwb8tiKNdVwXA59";
        const email = s.contactEmail || "kkrrppi@gmail.com";
        const phone = s.contactPhone || "(0354) 682433";
        const wa = s.contactWa || "6285800753804";
        const heroSubtitleLabel = s.heroSubtitleLabel || "Ibadah Kebaktian Kebangunan Rohani";

        // Hero heading
        if (s.heroHeading1 && safe("hero-line1")) safe("hero-line1").textContent = s.heroHeading1;
        if (s.heroHeading2 && safe("hero-line2")) safe("hero-line2").textContent = s.heroHeading2;
        if (s.heroHeading3 && safe("hero-line3")) safe("hero-line3").textContent = s.heroHeading3;

        // Hero subtitle label
        if (safe("hero-subtitle-label")) safe("hero-subtitle-label").textContent = heroSubtitleLabel;

        // Hero verse
        if (safe("hero-verse")) {
          const vt = s.heroVerse || (s.heroSubtitle ? s.heroSubtitle.split("—")[0].trim() : null) || "Percayalah kepada TUHAN dengan segenap hatimu dan janganlah bersandar kepada pengertianmu sendiri.";
          const ref = s.heroVerseRef || (s.heroSubtitle && s.heroSubtitle.includes("—") ? s.heroSubtitle.split("—")[1].trim() : null) || "Amsal 3:5";
          safe("hero-verse").textContent = vt;
          if (safe("hero-verse-ref")) safe("hero-verse-ref").textContent = "— " + ref;
        }

        // Hero additional titles (orphaned settings fix)
        const heroTitles = document.getElementById("hero-titles");
        if (heroTitles) {
          if (s.heroTitle1 && safe("hero-title1")) safe("hero-title1").textContent = s.heroTitle1;
          if (s.heroTitle2 && safe("hero-title2")) safe("hero-title2").textContent = s.heroTitle2;
          if (s.heroTitle1 || s.heroTitle2) heroTitles.classList.remove("hidden");
        }

        // Hero event info bar
        const infoBar = document.getElementById("hero-event-info");
        if (infoBar) {
          infoBar.innerHTML = [
            `<span class="flex items-center gap-1.5"><svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg> ${escapeHtml(date)}</span>`,
            `<span class="flex items-center gap-1.5"><svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg> ${escapeHtml(time)}</span>`,
            `<span class="flex items-center gap-1.5"><svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg> ${escapeHtml(venue)}</span>`
          ].join("");
        }

        // Event info card (about section sidebar)
        if (safe("event-card-date")) safe("event-card-date").textContent = date;
        if (safe("event-card-venue")) safe("event-card-venue").textContent = venue;
        if (safe("event-card-time")) safe("event-card-time").textContent = time;
        if (safe("event-card-place")) safe("event-card-place").textContent = venue.split(",")[0] || venue;
        if (s.eventCardBadge && safe("event-card-badge")) safe("event-card-badge").textContent = s.eventCardBadge;

        // About section label
        if (s.aboutSectionLabel && safe("about-section-label")) safe("about-section-label").textContent = s.aboutSectionLabel;

        // About text
        if (s.aboutTitle && safe("about-heading")) safe("about-heading").textContent = s.aboutTitle;
        if (s.aboutDesc1 && safe("about-desc1")) safe("about-desc1").textContent = s.aboutDesc1;
        if (s.aboutDesc2 && safe("about-desc2")) safe("about-desc2").textContent = s.aboutDesc2;

        // About cards (orphaned settings fix)
        const aboutCards = document.getElementById("about-cards");
        if (aboutCards) {
          if (s.aboutCard1Title && safe("about-card1-title")) safe("about-card1-title").textContent = s.aboutCard1Title;
          if (s.aboutCard1Desc && safe("about-card1-desc")) safe("about-card1-desc").textContent = s.aboutCard1Desc;
          if (s.aboutCard2Title && safe("about-card2-title")) safe("about-card2-title").textContent = s.aboutCard2Title;
          if (s.aboutCard2Desc && safe("about-card2-desc")) safe("about-card2-desc").textContent = s.aboutCard2Desc;
          if (s.aboutCard1Title || s.aboutCard2Title) aboutCards.classList.remove("hidden");
        }

        // About blockquote
        if (s.aboutBlockquote && safe("about-blockquote")) {
          const bq = s.aboutBlockquote;
          const parts = bq.split("—");
          safe("about-blockquote").innerHTML = (parts[0] ? `"${escapeHtml(parts[0].trim())}"` : "") + '<br/><span id="about-blockquote-ref" class="text-sm not-italic text-kkr-dark/60 font-medium">— ' + (parts[1] ? escapeHtml(parts[1].trim()) : "") + "</span>";
        }

        // Event info card labels
        if (s.eventCardLabelPada && safe("event-card-label-pada")) safe("event-card-label-pada").textContent = s.eventCardLabelPada;
        if (s.eventCardLabelPukul && safe("event-card-label-pukul")) safe("event-card-label-pukul").textContent = s.eventCardLabelPukul;
        if (s.eventCardLabelTempat && safe("event-card-label-tempat")) safe("event-card-label-tempat").textContent = s.eventCardLabelTempat;
        if (s.eventCardBrand && safe("event-card-brand")) safe("event-card-brand").textContent = s.eventCardBrand;

        // Location sub-labels
        if (s.locSubLabelAlamat && safe("loc-label-alamat")) safe("loc-label-alamat").textContent = s.locSubLabelAlamat;
        if (s.locSubLabelTanggal && safe("loc-label-tanggal")) safe("loc-label-tanggal").textContent = s.locSubLabelTanggal;
        if (s.locSubLabelWaktu && safe("loc-label-waktu")) safe("loc-label-waktu").textContent = s.locSubLabelWaktu;
        if (s.locSubLabelBatasDaftar && safe("loc-label-batas-daftar")) safe("loc-label-batas-daftar").textContent = s.locSubLabelBatasDaftar;
        if (s.locSubLabelKontak && safe("loc-label-kontak")) safe("loc-label-kontak").textContent = s.locSubLabelKontak;
        if (s.locBtnMaps && safe("loc-btn-maps-text")) safe("loc-btn-maps-text").textContent = s.locBtnMaps;

        // CTA contact label
        if (s.ctaContactLabel && safe("cta-contact-label")) safe("cta-contact-label").textContent = s.ctaContactLabel;

        // CTA
        if (s.ctaTitle && safe("cta-title")) safe("cta-title").textContent = s.ctaTitle;
        if (s.ctaDesc && safe("cta-desc")) safe("cta-desc").textContent = s.ctaDesc;

        // Section labels and headings
        if (s.sectionLabelSpeakers && safe("section-label-speakers")) safe("section-label-speakers").textContent = s.sectionLabelSpeakers;
        if (s.sectionHeadingSpeakers && safe("section-heading-speakers")) safe("section-heading-speakers").textContent = s.sectionHeadingSpeakers;
        if (s.sectionLabelStory && safe("section-label-story")) safe("section-label-story").textContent = s.sectionLabelStory;
        if (s.btnSpeakerPrev && safe("speaker-grid")) safe("speaker-grid").dataset.btnPrev = s.btnSpeakerPrev;
        if (s.btnSpeakerNext && safe("speaker-grid")) safe("speaker-grid").dataset.btnNext = s.btnSpeakerNext;
        if (s.sectionLabelAltar && safe("section-label-altar")) safe("section-label-altar").textContent = s.sectionLabelAltar;
        if (s.sectionHeadingAltar && safe("section-heading-altar")) safe("section-heading-altar").textContent = s.sectionHeadingAltar;
        if (s.sectionDescAltar && safe("section-desc-altar")) safe("section-desc-altar").textContent = s.sectionDescAltar;
        if (s.sectionLabelSponsors && safe("section-label-sponsors")) safe("section-label-sponsors").textContent = s.sectionLabelSponsors;
        if (s.sectionHeadingSponsors && safe("section-heading-sponsors")) safe("section-heading-sponsors").textContent = s.sectionHeadingSponsors;
        if (s.sectionDescSponsors && safe("section-desc-sponsors")) safe("section-desc-sponsors").textContent = s.sectionDescSponsors;
        if (s.sectionLabelLocation && safe("section-label-location")) safe("section-label-location").textContent = s.sectionLabelLocation;
        if (s.sectionLabelCta && safe("section-label-cta")) safe("section-label-cta").textContent = s.sectionLabelCta;

        // Button texts
        if (s.btnHeroPrimary && safe("btn-hero-primary")) safe("btn-hero-primary").textContent = s.btnHeroPrimary;
        if (s.btnHeroSecondary && safe("btn-hero-secondary")) safe("btn-hero-secondary").textContent = s.btnHeroSecondary;
        if (s.btnSponsorContact && safe("btn-sponsor-contact")) safe("btn-sponsor-contact").textContent = s.btnSponsorContact;

        // Location section
        if (safe("loc-venue")) safe("loc-venue").textContent = venue;
        if (safe("loc-address")) safe("loc-address").textContent = s.locAddress || "Lihat peta di bawah";
        if (safe("loc-date")) safe("loc-date").textContent = date;
        if (safe("loc-time")) safe("loc-time").textContent = time;
        if (safe("reg-deadline")) safe("reg-deadline").textContent = regDeadline;
        if (safe("loc-phone")) safe("loc-phone").textContent = phone;
        if (safe("loc-wa")) { safe("loc-wa").href = "https://wa.me/" + wa; safe("loc-wa").textContent = "WhatsApp: +" + wa; }
        if (safe("loc-maps-btn")) safe("loc-maps-btn").href = sanitizeUrl(mapsLink);
        
        // Map iframe: prioritize admin embed URL, fallback to geocode, then to Google Maps link embed
        if (safe("loc-maps-iframe")) {
          const iframe = safe("loc-maps-iframe");
          if (s.locMaps) {
            // Admin provided exact Google Maps embed URL - use directly
            iframe.src = s.locMaps;
          } else if (mapsLink) {
            // No custom embed, but we have Google Maps link - try geocode first
            setEmbedMap(venue, mapsLink).then(({url}) => {
              if (url && iframe) iframe.src = url;
              else if (iframe && mapsLink) {
                // Geocode failed, try to construct Google Maps embed from share link
                // Extract place_id or coords from Google Maps link if possible
                try {
                  const u = new URL(mapsLink);
                  if (u.hostname.includes("google")) {
                    // Use the maps link as-is for an embed attempt
                    // Note: This won't work directly as embed, but geocode should handle it
                    if (!iframe.src) iframe.src = "";
                  }
                } catch {}
              }
            });
          } else {
            // No maps link at all - use geocode with venue
            setEmbedMap(venue, "");
          }
        }

        // CTA contact
        if (safe("cta-email")) safe("cta-email").textContent = email;
        if (safe("cta-phone")) safe("cta-phone").textContent = phone;
      } catch {}
    }

    async function loadSpeakers() {
      try {
        const res = await fetch("/api/speakers");
        const d = await res.json();
        const container = document.getElementById("speaker-grid");
        const list = (d.speakers || []).filter(s => s.is_active);
        document.getElementById("speaker-skeleton")?.classList.add("hidden");
        if (!list.length) {
          container.innerHTML = `<div class="text-center py-12 text-white/50"><p class="text-sm">Belum ada pembicara.</p></div>`;
          return;
        }

        let currentIndex = 0;
        const storyLabel = (document.getElementById("section-label-story")?.textContent) || "Story Pembicara";
        const btnPrev = container.dataset.btnPrev || "Sebelumnya";
        const btnNext = container.dataset.btnNext || "Selanjutnya";

        function renderSpeaker(index) {
          const s = list[index];
          const total = list.length;
          const heading = safe("section-heading-speakers");
          if (heading) heading.textContent = "Menghadirkan " + (s.title ? s.title.charAt(0).toUpperCase() + s.title.slice(1) + " " : "") + (s.name || "");
          container.innerHTML = `
            <div class="glass-card rounded-3xl p-8 sm:p-10 md:p-12">
              <div class="grid gap-8 sm:grid-cols-[1fr_1.8fr] sm:items-center">
                <div class="flex flex-col items-center sm:items-start text-center sm:text-left">
                  ${s.photo_url
                    ? `<img src="${escapeHtml(sanitizeUrl(s.photo_url))}" loading="lazy" decoding="async" class="w-40 h-40 sm:w-48 sm:h-48 md:w-56 md:h-56 rounded-2xl object-cover shadow-xl ring-2 ring-white/20" alt="${escapeHtml(s.name)}" />`
                    : `<div class="w-40 h-40 sm:w-48 sm:h-48 md:w-56 md:h-56 rounded-2xl bg-gradient-to-br from-kkr-light/80 to-white flex items-center justify-center shadow-xl ring-2 ring-white/20"><svg xmlns="http://www.w3.org/2000/svg" class="w-20 h-20 text-kkr-red/60" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg></div>`
                  }
                  ${s.title ? `<span class="inline-block text-[11px] font-semibold uppercase tracking-wider text-kkr-red bg-kkr-light/80 px-3 py-1 rounded-full mt-5">${escapeHtml(s.title)}</span>` : ""}
                  <h3 class="text-2xl sm:text-3xl font-bold text-kkr-dark mt-2">${escapeHtml(s.name)}</h3>
                  ${s.organization ? `<p class="text-base text-kkr-dark/60 mt-1">${escapeHtml(s.organization)}</p>` : ""}
                  ${s.tags && typeof s.tags === 'string' ? `<div class="flex flex-wrap gap-2 mt-4 justify-center sm:justify-start">${s.tags.split(",").map(t => `<span class="text-[11px] bg-kkr-light text-kkr-red px-3 py-1 rounded-full font-semibold">${escapeHtml(t.trim())}</span>`).join("")}</div>` : ""}
                  ${s.description ? `<p class="mt-4 text-sm text-kkr-dark/70 leading-relaxed">${escapeHtml(s.description)}</p>` : ""}
                </div>
                <div class="relative flex flex-col justify-center ${s.story ? '' : 'items-center justify-center'}">
                  ${s.story ? `
                    <svg class="absolute -top-2 -left-1 w-9 h-9 text-kkr-red/15" viewBox="0 0 24 24" fill="currentColor"><path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10H14.017zM0 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151C7.546 6.068 5.983 8.789 5.983 11H10v10H0z"/></svg>
                    <p class="text-sm sm:text-base text-kkr-dark/80 leading-8 italic pl-10">${escapeHtml(s.story)}</p>
                  ` : `<p class="text-sm text-kkr-dark/40 italic">Belum ada kisah.</p>`}
                </div>
              </div>
              ${total > 1 ? `
              <div class="flex items-center justify-center gap-5 mt-8 pt-5 border-t border-kkr-light/40">
                <button id="sp-prev" class="rounded-full bg-kkr-light/60 hover:bg-kkr-light px-5 py-2.5 text-sm font-semibold text-kkr-dark transition active:scale-95" ${index === 0 ? 'disabled style="opacity:0.3;cursor:not-allowed"' : ''}>
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 inline -ml-0.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
                  ${escapeHtml(btnPrev)}
                </button>
                <span class="text-sm text-kkr-dark/40 font-semibold tabular-nums">${index + 1} / ${total}</span>
                <button id="sp-next" class="rounded-full bg-kkr-light/60 hover:bg-kkr-light px-5 py-2.5 text-sm font-semibold text-kkr-dark transition active:scale-95" ${index === total - 1 ? 'disabled style="opacity:0.3;cursor:not-allowed"' : ''}>
                  ${escapeHtml(btnNext)}
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 inline -mr-0.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
                </button>
              </div>
              ` : ""}
            </div>
          `;
          if (total > 1) {
            container.querySelector("#sp-prev")?.addEventListener("click", () => {
              if (currentIndex > 0) { currentIndex--; renderSpeaker(currentIndex); }
            }, { once: true });
            container.querySelector("#sp-next")?.addEventListener("click", () => {
              if (currentIndex < total - 1) { currentIndex++; renderSpeaker(currentIndex); }
            }, { once: true });
          }
        }

        renderSpeaker(0);
      } catch {
        document.getElementById("speaker-grid").innerHTML = `<div class="text-center py-12 text-red-500"><p>Gagal memuat pembicara.</p></div>`;
      }
    }

    function marquee(el) {
      if (!el) return;
      el.classList.add("mq-track");
      let pos = 0, running = true, id = null;
      function tick() {
        if (!running) return;
        pos -= 0.5;
        const full = el.scrollWidth / 2;
        if (pos <= -full || Math.abs(pos) >= full) { pos = 0; }
        el.style.transform = "translateX(" + pos + "px)";
        id = requestAnimationFrame(tick);
      }
      el.addEventListener("mouseenter", function () {
        running = false;
        if (id) cancelAnimationFrame(id);
      });
      el.addEventListener("mouseleave", function () {
        if (id) cancelAnimationFrame(id);
        running = true;
        id = requestAnimationFrame(tick);
      });
      id = requestAnimationFrame(tick);
    }

    let allAltarServers = [];

    async function loadAltarServers() {
      try {
        const res = await fetch("/api/altar-servers");
        const d = await res.json();
        const container = document.getElementById("altar-grid");
        const list = (d.altarServers || []).filter(s => s.is_active);
        allAltarServers = list;
        document.getElementById("altar-skeleton")?.classList.add("hidden");
        if (!list.length) {
          container.innerHTML = `<div class="text-center py-12 text-white/50"><p class="text-sm">Belum ada pelayan altar.</p></div>`;
          return;
        }
        const makeCard = (baseClass) => s => `
          <div class="${baseClass}" data-id="${escapeHtml(String(s.id))}">
            ${s.photo_url
              ? `<img src="${escapeHtml(sanitizeUrl(s.photo_url))}" loading="lazy" decoding="async" class="w-20 h-20 rounded-full object-cover mx-auto shadow-md shadow-kkr-light/40 border-2 border-white" alt="${escapeHtml(s.name)}" />`
              : `<div class="w-20 h-20 rounded-full bg-gradient-to-br from-kkr-light/80 to-white mx-auto flex items-center justify-center shadow-md shadow-kkr-light/40"><svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-kkr-red/60" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg></div>`
            }
            <div class="mt-3 space-y-0.5">
              ${s.title ? `<span class="inline-block text-[10px] font-semibold uppercase tracking-wider text-kkr-red bg-kkr-light px-2 py-0.5 rounded-full">${escapeHtml(s.title)}</span>` : ""}
              <h3 class="text-base font-semibold text-kkr-dark mt-1">${escapeHtml(s.name)}</h3>
              ${s.organization ? `<p class="text-kkr-dark/70 text-xs">${escapeHtml(s.organization)}</p>` : ""}
            </div>
            ${s.description ? `<div class="mt-2"><p class="text-xs text-kkr-dark/80 leading-relaxed text-center max-w-[200px] mx-auto break-words">${escapeHtml(s.description)}</p></div>` : '<div class="flex-1"></div>'}
          </div>`;
        if (list.length > 3) {
          const base = "rounded-2xl glass-card p-4 sm:p-5 text-center hover:shadow-xl hover:-translate-y-0.5 transition-all duration-300 shrink-0 w-[200px] sm:w-[220px] flex flex-col cursor-pointer";
          const html = list.map(makeCard(base)).join("");
          container.innerHTML = `
            <div class="overflow-hidden" style="mask-image: linear-gradient(to right, transparent 0, black 40px, black calc(100% - 40px), transparent 100%);">
              <div class="flex gap-16 sm:gap-24" style="width: max-content;">
                <div class="flex gap-16 sm:gap-24">${html}</div>
                <div class="flex gap-16 sm:gap-24" aria-hidden="true">${html}</div>
              </div>
            </div>`;
          const track = container.querySelector(".overflow-hidden > div");
          if (track) marquee(track);
        } else {
          const base = "rounded-2xl glass-card p-5 sm:p-6 text-center hover:shadow-xl hover:-translate-y-0.5 transition-all duration-300 w-full flex flex-col cursor-pointer";
          const html = list.map(makeCard(base)).join("");
          if (list.length === 1) {
            container.innerHTML = `<div class="flex justify-center max-w-[280px] mx-auto">${html}</div>`;
          } else if (list.length === 2) {
            container.innerHTML = `<div class="grid gap-6 sm:grid-cols-2 max-w-xl mx-auto">${html}</div>`;
          } else {
            container.innerHTML = `<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 max-w-4xl mx-auto">${html}</div>`;
          }
        }
      } catch {
        document.getElementById("altar-grid").innerHTML = `<div class="text-center py-12 text-red-500"><p>Gagal memuat pelayan altar.</p></div>`;
      }
    }

    let allSponsors = [];

    async function loadSponsors() {
      try {
        const res = await fetch("/api/sponsors");
        const data = await res.json();
        allSponsors = data.sponsors || [];
        const list = allSponsors;
        if (!list.length) {
          document.getElementById("sponsor-grid").innerHTML = `<div class="text-center py-12 text-white/50"><p class="text-sm">Belum ada sponsor. Jadilah yang pertama!</p></div>`;
          return;
        }
        document.getElementById("sponsor-skeleton")?.classList.add("hidden");
        const card = (s) =>
          `<div class="sponsor-card shrink-0 cursor-pointer transition-all duration-300 hover:scale-110 active:scale-95" data-id="${s.id}">
            ${s.logo_url ? `<img src="${escapeHtml(sanitizeUrl(s.logo_url))}" loading="lazy" decoding="async" class="w-56 h-56 sm:w-64 sm:h-64 object-contain" alt="${escapeHtml(s.name)}" />` : `<div class="w-56 h-56 sm:w-64 sm:h-64 flex items-center justify-center"><svg xmlns="http://www.w3.org/2000/svg" class="w-16 h-16 text-white/50" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg></div>`}
          </div>`;
        const html = list.map(card).join("");
        if (list.length <= 3) {
          const container = document.getElementById("sponsor-grid");
          container.innerHTML = '';
          container.style.maskImage = 'none';
          const gridCols = list.length === 1 ? "flex justify-center" : (list.length === 2 ? "grid grid-cols-1 sm:grid-cols-2 gap-10 max-w-2xl mx-auto" : "grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10 max-w-4xl mx-auto");
          const innerHtml = list.map(s => {
            const logo = s.logo_url
              ? `<img src="${escapeHtml(sanitizeUrl(s.logo_url))}" loading="lazy" decoding="async" class="w-56 h-56 sm:w-64 sm:h-64 object-contain" alt="${escapeHtml(s.name)}" />`
              : `<div class="w-56 h-56 sm:w-64 sm:h-64 flex items-center justify-center"><svg xmlns="http://www.w3.org/2000/svg" class="w-16 h-16 text-white/50" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg></div>`;
            return `<div class="sponsor-card cursor-pointer transition-all duration-300 hover:scale-110 active:scale-95 mx-auto" data-id="${s.id}">${logo}</div>`;
          }).join("");
          container.innerHTML = `<div class="${gridCols}">${innerHtml}</div>`;
        } else {
          document.querySelector('[data-copy="1"]').innerHTML = html;
          document.querySelector('[data-copy="2"]').innerHTML = html;
          const track = document.querySelector(".sponsor-track");
          track?.classList.remove("hidden");
          if (track) setTimeout(function() { marquee(track); }, 50);
        }
      } catch {
        document.getElementById("sponsor-grid").innerHTML = `<div class="text-center py-12 text-red-500"><p>Gagal memuat data sponsor.</p></div>`;
      }
    }

    /* Sponsor card click */
    document.addEventListener("click", (e) => {
      const card = e.target.closest(".sponsor-card");
      if (!card) return;
      const id = card.dataset.id;
      const sponsor = allSponsors.find(s => String(s.id) === String(id));
      if (!sponsor || !sponsor.website) return;
      try { window.open(sanitizeUrl(new URL(sponsor.website).href), "_blank", "noopener,noreferrer"); } catch {}
    });

    /* Altar card click */
    document.addEventListener("click", (e) => {
      const card = e.target.closest("#altar-grid [data-id]");
      if (!card) return;
      const id = card.dataset.id;
      const altar = allAltarServers.find(s => String(s.id) === String(id));
      if (!altar || !altar.website) return;
      try { window.open(sanitizeUrl(new URL(altar.website).href), "_blank", "noopener,noreferrer"); } catch {}
    });

    document.addEventListener("DOMContentLoaded", () => {
      Promise.all([loadSettings(), loadSpeakers(), loadAltarServers(), loadSponsors()]).catch(console.warn);
    });
  </script>
</PublicLayout>
